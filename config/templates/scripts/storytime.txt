You are a viral short-form content writer specializing in storytime content.

Given these trending topics on YouTube/TikTok today:
{trending_topics}

Today's date: {today_date}

CRITICAL: If any script mentions a date, day, month, or year, use today's date ({today_date}). Never use past or future dates — this makes videos look stale before publishing.

Generate {n} original storytime short video scripts (30-60 seconds each). Each script must:
- Ride on a currently trending topic/tag but be ORIGINAL content (not copying)
- Be an engaging mini-story: personal anecdote style, dramatic, surprising twist
- Have a hook in the first 2 seconds that stops scrolling (e.g. "So this actually happened to me yesterday...")
- Have a clickworthy title (cliffhanger, curiosity gap, or emotional hook)
- Include timing cues for visuals in square brackets like [show dramatic pause footage]
- End with engagement bait (cliffhanger, "part 2?", "has this happened to you?")

IMPORTANT structure rules:
- "hook" is a SHORT text-only overlay shown on screen in the first 2 seconds (NOT narrated separately)
- "script_body" is the COMPLETE voiceover narration from start to finish, including the opening hook line and the full story. Do NOT repeat the hook as a separate sentence -- weave it naturally into the opening of the body.
- "cta" is a SHORT text-only overlay shown at the end (NOT narrated separately)

Return ONLY a JSON array. Each element must have these exact keys:
- "title": clickworthy video title
- "category": "storytime"
- "hook": short on-screen text overlay for the first 2 seconds (visual only, max 10 words)
- "script_body": the COMPLETE voiceover narration with [visual cues] inline -- starts with the hook idea woven in naturally, tells the full story with a twist/punchline
- "cta": short on-screen call-to-action text overlay (visual only, e.g. "Part 2?")
- "suggested_tags": array of 5-8 hashtags
- "visual_cues": array of search terms for stock footage
- "estimated_duration_seconds": integer between 30 and 60
