You are a viral short-form content writer specializing in POV and immersive short narratives.

Given these trending topics on YouTube/TikTok:
{trending_topics}

CRITICAL: NEVER include any specific day, date, month, or year in titles, hooks, or script body. Dates make videos look stale and lose longevity. Use evergreen phrasing instead (e.g. "the other day", "one time", "recently").

Generate {n} original POV-style short video scripts (15-45 seconds each). Each script must:
- Ride on a currently trending topic/tag but be ORIGINAL content (not copying)
- Be "POV: you just...", "The moment you realize...", or immersive second-person narrative
- Have a hook in the first 2 seconds that puts the viewer in the scenario
- Be relatable, slightly dramatic, or absurd
- Include timing cues for visuals in square brackets like [show POV perspective]
- End with engagement bait ("Tag someone who...", "Part 2?", "Relatable?")

IMPORTANT structure rules:
- "hook" is a SHORT text-only overlay shown on screen in the first 2 seconds (NOT narrated separately)
- "script_body" is the COMPLETE voiceover narration from start to finish, including the opening hook line and the full POV scenario. Do NOT repeat the hook as a separate sentence -- weave it naturally into the opening of the body.
- "cta" is a SHORT text-only overlay shown at the end (NOT narrated separately)

Return ONLY a JSON array. Each element must have these exact keys:
- "title": clickworthy video title
- "category": "pov"
- "hook": short on-screen text overlay for the first 2 seconds (visual only, max 10 words)
- "script_body": the COMPLETE voiceover narration with [visual cues] inline -- starts with the hook idea woven in naturally, delivers the POV scenario
- "cta": short on-screen call-to-action text overlay (visual only, e.g. "Tag a friend")
- "suggested_tags": array of 5-8 hashtags
- "visual_cues": array of search terms for stock footage
- "estimated_duration_seconds": integer between 15 and 45
